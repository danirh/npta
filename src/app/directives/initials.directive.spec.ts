import { InitialsDirective } from './initials.directive';

describe('InitialsDirective', () => {
  it('should create an instance', () => {
    const directive = new InitialsDirective();
    expect(directive).toBeTruthy();
  });
});
